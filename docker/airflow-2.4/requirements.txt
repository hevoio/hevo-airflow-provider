# Apache Airflow 2.4.x Core Dependencies
apache-airflow==2.4.3
apache-airflow-core==2.4.3

# Apache Airflow Providers
apache-airflow-providers-http==4.1.0
apache-airflow-providers-mysql==3.4.0
apache-airflow-providers-apache-spark==4.0.0
apache-airflow-providers-snowflake==4.2.0

# Hevo Provider Dependencies
aiohttp==3.13.2
requests==2.32.5

# Hevo Airflow Provider (local)
# Will be installed from parent directory during Docker build
# Using: uv pip install -e /opt/hevo-airflow-provider

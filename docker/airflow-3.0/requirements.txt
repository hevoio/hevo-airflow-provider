# Apache Airflow 3.0.x is pre-installed in the base image: apache/airflow:3.0.6-python3.12

# Additional Apache Airflow Providers
apache-airflow-providers-fab
apache-airflow-providers-http==5.3.1
apache-airflow-providers-mysql==6.3.1
apache-airflow-providers-snowflake==6.7.0

# Hevo Provider Dependencies
aiohttp==3.13.2
pydantic>=2.0.0

# Hevo Airflow Provider (local)
# Will be installed from parent directory during Docker build
# Using: pip install -e /opt/hevo-airflow-provider
